"""파이프라인 전역 설정."""

import os

# ----------------------------------------------------------------------
# 경로
# ----------------------------------------------------------------------
RAW_CSV = "./data_raw/train.csv"
ARTIFACT_DIR = "./artifacts"
RESULT_DIR = "./results"
FIGURE_DIR = "./figures"

for _d in (ARTIFACT_DIR, RESULT_DIR, FIGURE_DIR):
    os.makedirs(_d, exist_ok=True)

# ----------------------------------------------------------------------
# 신호 처리
# ----------------------------------------------------------------------
SEGMENT_SIZE = 150_000        # 4 MHz 기준 37.5 ms
SAMPLING_RATE = 4_000_000
NPERSEG = 3000                # 주파수 bin 1501개
NOVERLAP = 1500               # 50% 중첩, 시간 프레임 101개
FREQ_BINS = 1501
TIME_STEPS = 101
MAX_SEGMENTS = 4195


def h5_path_for(n_segments):
    """파일명에 세그먼트 수를 포함시켜, 다른 조건으로 만든 데이터셋이
    의도치 않게 재사용되는 것을 막는다."""
    return f"./data_processed_n{n_segments}.h5"


H5_PATH = h5_path_for(MAX_SEGMENTS)

# 기계 소음을 모사한 합성 저주파 간섭.
# 실측 TBM 스펙트럼이 아니라 가상 시나리오이다.
NOISE_BAND_BINS = 8           # 0-10.66 kHz
NOISE_MEAN = 12.0
NOISE_STD = 1.5
NOISE_SEED = 20240101

# ----------------------------------------------------------------------
# 라벨
# ----------------------------------------------------------------------
WARNING_TTF_THRESHOLD = 3.0   # TTF 3초 이하를 경보 클래스로 정의
THRESHOLD_SENSITIVITY = [2.0, 3.0, 5.0]

# ----------------------------------------------------------------------
# 전조 상태 벡터
# ----------------------------------------------------------------------
# 인덱스 0-3은 단일 37.5 ms 세그먼트 내부에서 계산되고, 인덱스 4-5는 직전
# TREND_WINDOW개 세그먼트(약 0.75초)를 요약하므로 세그먼트 단위 스펙트로그램
# 인코더가 접근할 수 없는 정보이다.
STATE_FEATURE_NAMES = [
    "rms_energy",
    "kurtosis",
    "peak_amplitude",
    "crest_factor",
    "energy_trend_slope",
    "cumulative_energy",
]
STATE_DIM = len(STATE_FEATURE_NAMES)
INTRA_SEGMENT_IDX = [0, 1, 2, 3]
INTER_SEGMENT_IDX = [4, 5]
TREND_WINDOW = 20

# 그림과 표에 사용할 표기
STATE_FEATURE_LABELS = {
    "rms_energy": "RMS energy",
    "kurtosis": "Kurtosis",
    "peak_amplitude": "Peak amplitude",
    "crest_factor": "Crest factor",
    "energy_trend_slope": "Energy trend slope",
    "cumulative_energy": "Cumulative energy",
}
ABLATION_LABELS = {
    "full": "Full state vector",
    "drop_inter_segment": "w/o inter-segment (both)",
    "drop_intra_segment": "w/o intra-segment (all four)",
    "drop_all": "w/o state vector",
}

# ----------------------------------------------------------------------
# 데이터 분할
# ----------------------------------------------------------------------
CYCLE_JUMP_THRESHOLD = 1.0    # TTF 불연속으로 새 스틱슬립 사이클을 판별
EXCLUDE_INCOMPLETE_CYCLES = True
TRAIN_RATIO = 0.60
VAL_RATIO = 0.15
MIN_TEST_POSITIVES = 30

USE_CROSS_VALIDATION = False
CV_N_FOLDS = 4
CV_TEST_CYCLES = 2
CV_VAL_CYCLES = 2

# ----------------------------------------------------------------------
# 타깃 정규화
# ----------------------------------------------------------------------
# 원시 TTF는 0-16초 범위여서 그 MSE가 복합 손실을 지배하고 하이퍼파라미터
# 탐색을 왜곡한다. 학습 시에는 [0, 1]로 정규화하고 보고 시에는 초 단위로
# 되돌린다.
NORMALIZE_TTF = True
HP_SELECTION_METRIC = "composite"   # "composite"(검증 PR-AUC + R2) 또는 "val_loss"

# ----------------------------------------------------------------------
# 학습
# ----------------------------------------------------------------------
BATCH_SIZE = 8
EPOCHS = 15
TUNE_EPOCHS = 8
NUM_WORKERS = 2
PATIENCE = 5
GRAD_CLIP = 5.0

HP_GRID = {
    "lr": [1e-3, 3e-4],
    "ttf_weight": [0.25, 1.0, 4.0],
}

FOCAL_ALPHA = 0.25
FOCAL_GAMMA = 2.0
LAMBDA_PHYSICS = 0.15
PURE_AE_BASELINE = True       # 스펙트로그램 전용 베이스라인은 에너지 가중항을 제외

# 결합 방식 변형은 경쟁 베이스라인이 아니라 제안 모델의 융합 ablation으로
# 보고한다. 비활성화하면 어텐션의 필요성을 판단할 근거가 사라진다.
INCLUDE_FUSION_ABLATION = True

# ----------------------------------------------------------------------
# 시드
# ----------------------------------------------------------------------
TUNE_SEED = 2024              # 평가 시드와 분리
EVAL_SEEDS = [11, 22, 33, 44, 55, 66, 77, 88, 99, 101, 202, 303, 404, 505, 606]
ABLATION_SEEDS = [11, 22, 33, 44, 55, 66, 77, 88]

# 고전 추정기는 결정론적이므로, 학습 집합을 부트스트랩 재표집하여 대응 검정에
# 필요한 분산을 부여한다.
CLASSICAL_BOOTSTRAP = True

# ----------------------------------------------------------------------
# 운영점
# ----------------------------------------------------------------------
TARGET_FAR_PCT = 15.0
FAR_SAFETY_FACTOR = 0.6       # 분포 이동을 상쇄하기 위해 검증에서 더 엄격한 FAR을 겨냥
THRESHOLD_BOOTSTRAP_N = 300
MATCHED_FAR_GRID = [5.0, 10.0, 15.0, 20.0]   # 동일 FAR 재현율 비교용

PRIMARY_CLS_METRIC = "pr_auc"
BOOTSTRAP_N = 1000

# ----------------------------------------------------------------------
# 그림 서식
# ----------------------------------------------------------------------
FIG_DPI = 600
FIG_FONT = ["Arial", "Helvetica", "DejaVu Sans"]
FIG_BASE_FONTSIZE = 8
FIG_SINGLE_COL_IN = 3.5       # 단칼럼 폭
FIG_DOUBLE_COL_IN = 7.2       # 양칼럼 폭

# Okabe-Ito 색맹 안전 팔레트
PALETTE = {
    "proposed": "#0072B2",
    "spectrogram_only": "#D55E00",
    "no_attention": "#009E73",
    "gbm": "#CC79A7",
    "logreg": "#666666",
    "neutral": "#999999",
    "highlight": "#B22222",
}
